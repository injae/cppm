
bi:
    - cppm build install 

ba:
    - cppm nieel 
    - cppm build install 
    - cppm nieel
            
bx:
    - cppm build install
    - cd ../xxx
    - cppm build
              
clean: 
    - cd bin 
    - sudo rm -rf *

nieel:
    - cd ../nieel
    - cppm build install 
    
nieelt:
    - cd ../nieel 
    - ./run.sh
cp:
    - cp cmake/project_maker.cmake ../nieel/cmake
    - cp cmake/cmake_option.cmake  ../nieel/cmake
    - cp cmake/library_maker.cmake  ../nieel/cmake

shared-lib-setting: 
    - LD_LIBRARY_PATH=/usr/local/lib

nieel-push:
    - cd ../nieel
    - git add *
    - git commit

push:
    - git add *
    - git commit 
    
ldd:
    - cd bin 
    - ldd cppm
    
xxx:
    - cd ../xxx 
    - cppm build 