[package]
name = "cpptoml"
# if you use git repo, package version is latest
version = "0.4.0"
description = "c++ toml parsing library"
global=false
# library name in cmake, cppm.toml use this value 
# [dependencies]
#${library_name} = {module =${value}}
cmake = {name = "cpptoml", findlib=""}
# write download value git or url
#download = {git=""}
download = {url="https://github.com/injae/cpptoml/archive/0.4.0.tar.gz"}
[dependencies]
