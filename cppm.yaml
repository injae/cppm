project:
    name: cppm
    version: 1.0.0
    authors: 8687lee@gmail.com 
     
    thirdparty:
        boost: 
            build-type: static
            version: 1.54.0
        mysql:
            build-type: static
        ormpp:
            build-type: header_only
        iguana:
            build-type: header_only 
        yaml-cpp:
            build-type: static
        protobuf:
            build-type: static
            version: 3.5.1

    include: cd include && ls
    src    : cd src && ls 
    list   : ls
     
    
    
    
    
    